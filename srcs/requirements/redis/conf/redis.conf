bind 0.0.0.0
port 6379

# Cache only, no persistence on disk
save ""
appendonly no

# policy adapted to a WordPress cache
maxmemory 128mb
maxmemory-policy allkeys-lru

# Logging on stdout for `docker logs`
logfile ""
